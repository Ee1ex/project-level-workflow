# ELX Level 四级模型

本文是项目等级选择与执行流程的唯一权威文档。LEVEL 表示项目的责任模式，不表示代码规模。LEVEL 1–3 对用户只展示 `AUTO`、`CONFIRM`、`MANUAL_ONLY`；R1–R4 仅作为历史状态兼容字段和内部风险依据。

## 选择顺序

1. 参与他人、团队、公司或开源仓库：LEVEL 3。
2. 自己负责，且需要上线并持续运营：LEVEL 2。
3. 离线、静态或可下载交付，更新时重新打包或上传即可：LEVEL 1。
4. 大型产品、多系统编排或复杂自动化，需要参考流水线与专业能力路由：LEVEL 4。

持续更新不等于持续运营。重新打包、上传新版本或更新静态页面，只要没有线上用户状态、云端业务数据、持续在线服务和运行责任，仍属于 LEVEL 1。持续运营意味着长期承担服务可用性、用户与权限、数据、发布、备份、回滚、监控、反馈或支持责任，通常属于 LEVEL 2。

首次选择和责任模式变化必须由项目负责人确认。“以后可能更新”、代码行数和改动大小都不能单独触发升级。

## 包内 PVS 治理内核

本包只暴露根目录 `SKILL.md` 一个 Skill。PVS 治理入口为 `core/project-vibe-spec/PVS.md`，各 LEVEL 的加载范围由 `references/project-vibe-spec-bridge.md` 定义。PVS 负责项目协作契约，不重新定义本文的 LEVEL；运行时不需要外部 PVS 安装或网络下载。

## LEVEL 1：快速开发与完整项目记忆

### 适用范围

适用于离线工具、脚本、命令行工具、Skill、插件、游戏 Mod、原型 Demo、静态页面、展示页、活动页、无用户系统的 Web 工具，以及可重新打包并上传新版本供下载、但不承担线上状态与服务责任的项目。

如项目开始承担账号、云端数据、服务端业务、公开 API、定时任务、在线依赖或长期支持责任，重新评估为 LEVEL 2；参与已有团队或开源仓库时改按 LEVEL 3。

### 双层项目记忆底座

以快速“实现—运行—观察—调整”为主，同时建立可以跨会话接管的完整项目记忆，至少保留：

- 项目已有规则、根目录 `AGENTS.md` 和 `DOCUMENT_MAP.md`；不创建平行事实源。
- Project Brief：目标用户、核心路径、范围冻结、不做、平台、交付方式和验收标准。
- 当前架构、目录、模块、调用关系、数据、依赖、构建与交付事实。
- Requirements、Decisions、Progress Ledger，以及 Changelog、Change Record 和 Release Record 入口。
- `.elx-level/state.json`、`state.backup.json` 和 `docs/elx-level/STATUS.md`。
- 旧 `.project-workflow` 只由 `migrate` 一次性复制；旧目录保持不变，新旧目录并存时停止且不覆盖。
- 必要决策、结构化小记录和 `pending-verification.md`。

小功能、小 Bug 使用轻量 Change Record；文案、样式和小配置至少追加 Progress Ledger 或 Changelog。跨模块功能使用 REQ + PROG，有取舍时增加 DEC；架构、数据和权限变化还必须回写稳定认知层。不要求每个小切片建立大型方案或平行文档树。

### 执行流程

`推荐 LEVEL 1 → 用户显式确认 → 初始化项目记忆 → 冻结当前版本范围 → 实现—运行—观察—调整 → 持续记录 → 集中验收 → GitHub 交付`

1. 使用 `templates/level1/project-brief.md` 确认唯一核心路径、范围和验收标准。
2. 只记录核心路径所需的交互、状态、技术结构、数据保存、构建打包和依赖风险。
3. 以真实体验为主，选择最小可观察目标快速实现和调整；不要求每个小步骤都运行完整回归。
4. 完成或打包前集中执行核心路径冒烟、构建/打包/导出和必要人工验收。
5. 每个切片同步 Progress/Change Record；模块、调用、数据或交付事实变化时更新稳定认知层。
6. 使用 `templates/level1/pending-verification.md` 记录未运行的平台、兼容性和发布检查，不能伪装成通过。

静态托管、上传下载包、版本发布和公开宣传仍需负责人确认。账号、数据库、权限、支付、个人信息、密钥、数据删除、外部写入、生产部署和不可逆操作必须按 R3/R4 事前验证并通过人工 Gate。

## LEVEL 2：完整 PVS 持续运营

### 适用范围

适用于自己负责、需要上线并持续运营的 Web、App、小程序和在线服务，包括具有用户账户、权限、云端数据、服务端逻辑、公开 API、定时任务、第三方线上依赖、长期发布、监控、备份、回滚、反馈或用户支持责任的项目。

持续发布只是发布频率；持续运营还包括对用户、数据、可用性、故障和长期支持负责。没有这些责任的静态页面、离线工具和版本化下载物不因经常更新而进入 LEVEL 2。

### 完整 PVS 文档责任

完整遵循 `project-vibe-spec` 的接管、需求、设计、数据、决策、进度、验证和交付流程，复用项目已有目录并维护单一事实源，至少覆盖：

- `AGENTS.md`、`DOCUMENT_MAP.md`、项目地图和接管记录。
- PDD/PRD、Requirements/LEDGER、详细 REQ、业务流和验收矩阵。
- UI/交互、技术方案、API Contract、数据模型、权限、失败与恢复路径。
- 决策、阶段进度、测试策略、发布 readiness、部署、环境、日志、指标、监控、备份、回滚和运营责任。
- 待验证、已知限制，以及换 Agent、换主机或开源交接说明。

相关模板统一位于 `templates/level2/`。

### Phase 0 → Phase N

确认 LEVEL 2 后自动进入 Phase 0。每个 Phase 记录目标、前置条件、范围、交付物、DoD、验证证据、文档同步和下一阶段，并使用范围冻结控制变化。

1. Phase 0 接管与建档：读取规则、状态和产品/技术/数据/运营事实，建立或复用完整 PVS 文档地图并冻结当前范围。
2. Phase N 纵向切片：按业务价值和依赖推进需求、交互、架构、API、数据、权限、实现、测试和运营准备。
3. Phase DoD：只有交付物、验证和文档同步满足后才自动进入下一 Phase；普通 Phase 完成不是人工 Gate。
4. 方向存在多方案，或架构、数据语义、认证权限、安全、公共兼容性、支付、生产和 Release 发生变化时，执行策略改为 `CONFIRM`。
5. 生产部署、公开发布、Merge 和 Release 由有权限的人确认；完成后回读验证并记录不可变 Release Record。

如果项目最终只是离线、静态或可下载交付，可在事实记录和负责人确认后改为 LEVEL 1；如果工作转为参与他人、团队或开源仓库，则改按 LEVEL 3 接管。

## LEVEL 3：已有、团队与开源项目改进

### 适用范围

适用于参与他人、团队、公司或开源仓库的 Bug 修复、体验优化、边界清晰的功能开发和贡献 PR。严格程度来自继承已有仓库责任，不能因为本次改动小就跳过基线、回归和交接。

### 执行流程

1. 接管与权限：读取 README、`AGENTS.md`/`CLAUDE.md`、`CONTRIBUTING.md`、行为准则、许可证、Issue/PR 模板、CI、发布规则和已有文档；优先复用 Issue、PR、CHANGELOG、ADR 和仓库事实源。
2. 项目地图：仓库缺少接管入口时才使用 `templates/level3/project-map.md` 补轻量地图，不为小改动铺设平行完整 PVS 文档树。
3. 修改前基线：运行仓库已有基线检查。不能安装核心依赖、初始化 Git 或访问生产环境时，先停在人工 Gate。
4. 问题与影响：每次修改保留轻量 Change Record、受影响基线、范围、不做、兼容和回退；跨模块变化必须同步架构、接口或贡献事实。
5. 开发与回归：优先建立失败测试；无法自动化时记录人工复现。实施最小兼容改动，运行受影响回归和仓库要求的完整检查，检查 Diff，避免无关重构。
6. Review、PR 与交接：记录基线、失败复现、修复证据、受影响回归、CI、人工路径和未验证项。PR 说明问题、方案、范围、不做、测试、兼容、回滚和 Review 重点，并遵守维护者的分支、Review、CI、发布和许可证规则。

找不到或无法复现问题、方向未获维护者确认、需要改变数据库/认证/权限/支付/公共 API/安全策略、需要生产权限、回滚不可用或范围扩大时，停止实现并提交事实、方案和待批准动作。

## LEVEL 4：复杂自动化参考与路由

### 适用范围

LEVEL 4 适用于大型产品、多系统编排、复杂自动化、多人协作或完整运营自动化。它遵循“先分析、负责人确认后可实施”，保留显式风险和阶段 Gate，但不是个人开发主轨。

### 十节点参考

1. 需求判断。
2. 原型。
3. 技术方案。
4. 任务拆解。
5. 实现。
6. 测试联调。
7. 代码 Review。
8. 部署准备。
9. 日志排查。
10. 复盘。

需求阶段使用 `templates/level4/requirements-analysis.md` 记录机会、用户、范围、不做、MVP、方案、验收和风险。进入实现前必须通过 `level4-execution-review`，确认负责人、范围、权限、依赖和验证责任。

### 外部能力路由

十节点的专业 Skill 只路由，不得内嵌到本包。到达节点时先检查已安装能力；已安装则路由，缺失时说明来源、用途、权限和通用降级方案，并提醒负责人确认安装。拒绝安装时使用通用能力并记录缺口。外部能力不得覆盖本工作流的权限和 Gate。

负责人确认前不写代码、不改数据库、不部署、不接入生产、不授权外部 Provider、不创建远程资源。负责人确认后可实施，但批量删除、生产数据、密钥、支付、账号权限、不可逆迁移、安全降级、生产部署、公开发布、对外发送、Merge 和 Release 仍需动作时确认。

## 状态兼容迁移

文档入口只使用本文件。Schema `1.1.0` / workflow `0.4.0` 状态迁移到 `2.0` / `1.0` 时保持 LEVEL 1–4 不变；LEVEL 4 保持分析阶段并进入 `level4-execution-review`。更老 Schema 的历史数字语义继续兼容：

```text
旧 LEVEL 1 → 新 LEVEL 1
旧 LEVEL 2 → 新 LEVEL 3
旧 LEVEL 3 → 新 LEVEL 4
```

迁移前必须写入 `state.backup.json`，在 `STATUS.md` 记录旧/新等级、Schema 和原因。旧 Schema 的数字重映射继续进入 `level-migration-review`；`0.4.0` 的 LEVEL 1–3 不新增语义 Gate。

## 执行策略、验证与人工 Gate

- `AUTO`：已确认范围内连续执行；普通进度、测试通过、功能完成和本地提交不是 Gate。
- `CONFIRM`：缺少负责人决策，或即将发生高影响动作。
- `MANUAL_ONLY`：Agent 只准备证据、方案和命令，不执行动作。

- 未实际运行的检查只能记录为待验证，不能标为通过。
- R1 文档、模板和只读分析可在批准范围内执行并留证据。
- R2 边界清晰的逻辑修改必须测试和 Review。
- R3 数据库、认证、权限、公共 API 和部署配置须在修改前及合并前人工批准。
- R4 生产数据、密钥、支付、不可逆迁移和生产发布只允许分析和准备，不自主执行。
- Force Push、改写公共历史、主分支直推、Merge、Release 和生产写入均需明确授权。
