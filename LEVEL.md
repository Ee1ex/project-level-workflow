# Project Level Workflow 四级模型

本文是项目等级选择与执行流程的唯一权威文档。LEVEL 表示项目的责任模式，不表示代码规模；R1–R4 表示当前动作风险，两者必须分别判断。

## 选择顺序

1. 参与他人、团队、公司或开源仓库：LEVEL 3。
2. 自己负责，且需要上线并持续运营：LEVEL 2。
3. 离线、静态或可下载交付，更新时重新打包或上传即可：LEVEL 1。
4. 大型产品、多系统编排或复杂自动化，当前只进入分析：LEVEL 4。

持续更新不等于持续运营。重新打包、上传新版本或更新静态页面，只要没有线上用户状态、云端业务数据、持续在线服务和运行责任，仍属于 LEVEL 1。持续运营意味着长期承担服务可用性、用户与权限、数据、发布、备份、回滚、监控、反馈或支持责任，通常属于 LEVEL 2。

首次选择和责任模式变化必须由项目负责人确认。“以后可能更新”、代码行数和改动大小都不能单独触发升级。

## LEVEL 1：快速验证与轻量交付

### 适用范围

适用于离线工具、脚本、命令行工具、Skill、插件、游戏 Mod、原型 Demo、静态页面、展示页、活动页、无用户系统的 Web 工具，以及可重新打包并上传新版本供下载、但不承担线上状态与服务责任的项目。

如项目开始承担账号、云端数据、服务端业务、公开 API、定时任务、在线依赖或长期支持责任，重新评估为 LEVEL 2；参与已有团队或开源仓库时改按 LEVEL 3。

### 最小底座

使用 PVS-Lite，至少保留：

- 项目已有规则、根目录 `AGENTS.md` 和 `DOCUMENT_MAP.md`；不创建平行事实源。
- Project Brief：目标用户、核心路径、必做、不做、平台、交付方式和验收标准。
- `.project-workflow/state.json`、`state.backup.json` 和 `docs/project-workflow/STATUS.md`。
- 必要决策、风险和 `pending-verification.md`。

不默认要求完整 `Requirements/LEDGER.md`、逐条 REQ、全套 PDD/PRD、复杂业务流或每个小步骤的进度文档；需求跨模块、涉及持久化或风险较高时再补充。

### 执行流程

`目标澄清 → 轻量设计 → 实现—运行—观察—调整 → 集中验收 → 打包或交付`

1. 使用 `templates/level1/project-brief.md` 确认唯一核心路径、范围和验收标准。
2. 只记录核心路径所需的交互、状态、技术结构、数据保存、构建打包和依赖风险。
3. 以真实体验为主，选择最小可观察目标快速实现和调整；不要求每个小步骤都运行完整回归。
4. 完成或打包前集中执行核心路径冒烟、构建/打包/导出和必要人工验收。
5. 使用 `templates/level1/pending-verification.md` 记录未运行的平台、兼容性和发布检查，不能伪装成通过。

静态托管、上传下载包、版本发布和公开宣传仍需负责人确认。账号、数据库、权限、支付、个人信息、密钥、数据删除、外部写入、生产部署和不可逆操作必须按 R3/R4 事前验证并通过人工 Gate。

## LEVEL 2：可持续运营项目

### 适用范围

适用于自己负责、需要上线并持续运营的 Web、App、小程序和在线服务，包括具有用户账户、权限、云端数据、服务端逻辑、公开 API、定时任务、第三方线上依赖、长期发布、监控、备份、回滚、反馈或用户支持责任的项目。

持续发布只是发布频率；持续运营还包括对用户、数据、可用性、故障和长期支持负责。没有这些责任的静态页面、离线工具和版本化下载物不因经常更新而进入 LEVEL 2。

### 完整 PVS 文档责任

完整遵循 `project-vibe-spec` 的接管、需求、设计、数据、决策、进度、验证和交付流程，复用项目已有目录并维护单一事实源，至少覆盖：

- `AGENTS.md`、`DOCUMENT_MAP.md`、项目地图和接管记录。
- PDD/PRD、Requirements/LEDGER、详细 REQ、业务流和验收矩阵。
- UI/交互、技术方案、API Contract、数据模型、权限、失败与恢复路径。
- 决策、阶段进度、测试策略、发布 readiness、部署、环境、日志、指标、监控、备份、回滚和运营责任。
- 待验证、已知限制，以及换 Agent、换主机或开源交接说明。

相关模板统一位于 `templates/level2/`。

### Gate 流程

1. G0 接管与范围：读取规则、状态和产品/技术/数据/运营事实，确认负责人、允许与禁止范围。
2. G1 需求与体验：确认 PDD/PRD、REQ、核心路径、MVP、不做、验收、成功指标和隐私边界。
3. G2 技术、数据与安全：确认系统边界、API、数据、权限、依赖、失败处理、部署、测试、发布和回滚。数据库、迁移、认证、支付、密钥和生产数据必须有书面方案、备份和回退路径。
4. G3 实现：按功能集成或里程碑切片推进，维护任务、决策、REQ 和状态。不采用每改一步就跑大量完整测试的固定节奏；局部检查按需执行，系统测试集中在风险事件、功能集成、里程碑和版本完成阶段。
5. G4 验证与发布 readiness：按风险执行单元、集成、Contract、端到端、兼容、权限、安全、恢复和人工 UAT。认证、权限、支付、生产数据、迁移、密钥和发布必须在操作前针对性验证并经人工 Gate。
6. G5 上线、观察与运营：生产发布由有权限的人批准和执行；上线后验证核心路径并观察日志、错误率、延迟、资源、业务指标和用户反馈，达到阈值时按既定回滚计划止损。

如果项目最终只是离线、静态或可下载交付，可在事实记录和负责人确认后改为 LEVEL 1；如果工作转为参与他人、团队或开源仓库，则改按 LEVEL 3 接管。

## LEVEL 3：已有、团队与开源项目改进

### 适用范围

适用于参与他人、团队、公司或开源仓库的 Bug 修复、体验优化、边界清晰的功能开发和贡献 PR。严格程度来自继承已有仓库责任，不能因为本次改动小就跳过基线、回归和交接。

### 执行流程

1. 接管与权限：读取 README、`AGENTS.md`/`CLAUDE.md`、`CONTRIBUTING.md`、行为准则、许可证、Issue/PR 模板、CI、发布规则和已有文档；确认来源、目标分支、贡献协议、允许范围和外部写入权限。
2. 项目地图：使用 `templates/level3/project-map.md` 记录目标、技术栈、启动/测试/Lint/构建命令、关键调用链、数据边界、CI、发布、回滚和未知区域。
3. 修改前基线：运行仓库已有基线检查。不能安装核心依赖、初始化 Git 或访问生产环境时，先停在人工 Gate。
4. 问题与影响：使用 `templates/level3/change-proposal.md` 记录当前行为、期望行为、复现、根因、范围、不做、兼容和回退，并分析接口、数据、配置、依赖、平台、文档和测试影响。
5. 开发与回归：优先建立失败测试；无法自动化时记录人工复现。实施最小兼容改动，运行受影响回归和仓库要求的完整检查，检查 Diff，避免无关重构。
6. Review、PR 与交接：记录基线、失败复现、修复证据、受影响回归、CI、人工路径和未验证项。PR 说明问题、方案、范围、不做、测试、兼容、回滚和 Review 重点，并遵守维护者的分支、Review、CI、发布和许可证规则。

找不到或无法复现问题、方向未获维护者确认、需要改变数据库/认证/权限/支付/公共 API/安全策略、需要生产权限、回滚不可用或范围扩大时，停止实现并提交事实、方案和待批准动作。

## LEVEL 4：复杂项目需求分析

### 适用范围

LEVEL 4 是大型产品、多系统编排、复杂自动化、多人协作或完整运营自动化项目的预留编号。当前版本只做机会与需求分析，不把复杂度直接转换成开发任务。

### 分析输出

使用 `templates/level4/requirements-analysis.md` 输出：

- 机会与问题、目标用户、场景、现有替代方案和成功假设。
- 范围、明确不做、MVP/版本边界和验收标准。
- 业务流、系统边界，以及技术、数据、安全、运营和成本风险。
- 至少两个方案的比较、取舍、推荐选择和待确认事项。
- 后续进入完整 PVS 或其他责任模式前的前置条件。

### 禁止动作

在用户另行确认后续流程前，不写代码、不改数据库、不创建迁移、不部署、不接入生产、不授权外部 Provider、不创建自动化实现、不自动拆开发任务、不创建远程资源。Gate 报告必须区分已确认事实、假设、未知项、风险、可选方案、推荐决策和等待确认的动作。

## 状态兼容迁移

文档入口从本版本起只使用本文件；已有项目的状态数据仍按以下规则迁移：

```text
旧 LEVEL 1 → 新 LEVEL 1
旧 LEVEL 2 → 新 LEVEL 3
旧 LEVEL 3 → 新 LEVEL 4
```

迁移前必须写入 `state.backup.json`，在 `STATUS.md` 记录旧/新等级、Schema 和原因，并把 Gate 置为 `level-migration-review` 等待人工确认。旧 LEVEL 3 若要改为新 LEVEL 2，必须显式提供批准人和原因重新确认，不能自动映射或静默批准。

## 通用验证与人工 Gate

- 未实际运行的检查只能记录为待验证，不能标为通过。
- R1 文档、模板和只读分析可在批准范围内执行并留证据。
- R2 边界清晰的逻辑修改必须测试和 Review。
- R3 数据库、认证、权限、公共 API 和部署配置须在修改前及合并前人工批准。
- R4 生产数据、密钥、支付、不可逆迁移和生产发布只允许分析和准备，不自主执行。
- Force Push、改写公共历史、主分支直推、Merge、Release 和生产写入均需明确授权。
