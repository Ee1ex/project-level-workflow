# LEVEL 2：可持续运营项目开发流程

## 1. 文档定位

本流程适用于自己负责、需要上线并持续运营的 Web/App/小程序/在线服务，例如有用户账户、权限、云端数据、服务端业务逻辑、公开 API、定时任务、第三方线上依赖、长期发布、备份、回滚、监控、反馈或用户支持责任的项目。

“持续发布”只是发布频率；“持续运营”还包含对线上用户、数据、服务可用性、故障处理和长期支持负责。没有用户系统的静态页面、离线工具和版本化下载物不因未来可能更新而进入本流程。

本流程完整使用 [`references/project-vibe-spec-bridge.md`](references/project-vibe-spec-bridge.md) 中的 PVS 约定，并沿用原始 `project-vibe-spec` 的项目规则优先、文档复用和人工确认原则。

## 2. 完整 PVS 文档责任

按项目已有目录维护一份权威事实源，至少覆盖：

- `AGENTS.md`、`DOCUMENT_MAP.md`、项目地图和接管记录；
- PDD/PRD、Requirements/LEDGER.md、详细 REQ、业务流和验收矩阵；
- UI/交互规范、技术方案、API Contract、数据模型、权限、失败和恢复路径；
- 决策记录、阶段/功能进度、测试策略、发布 readiness、部署、环境、日志、指标、监控、备份、回滚和运营责任；
- 待验证事项、已知限制、交付报告和换 Agent/换主机/开源交接说明。

已有等价目录优先复用，不为了套模板创建平行 `docs/`、`Requirements/` 或 `Progress/`。

## 3. 阶段与人工 Gate

### G0：接管与范围确认

读取项目规则、文档地图、已有产品/技术/数据/运营事实和状态。记录目标、负责人、允许/禁止修改范围、依赖、数据边界、用户和支持责任。未解决的产品、权限、安全或架构歧义不能进入实现。

### G1：需求与体验确认

完成 PRD/PDD、REQ、核心用户路径、状态、MVP、版本范围、不做清单、验收标准、成功指标、隐私和待确认事项。需要页面时补 UI/交互规范、空/错/无权限/离线/过期状态。用户确认后才进入技术设计。

### G2：技术、数据与安全 Gate

写明系统边界、模块、API、数据实体、权限、第三方依赖、失败处理、容量、日志指标、部署环境、测试策略、发布和回滚。数据库表、字段、索引、约束、迁移、删除、认证、权限、支付、密钥和生产数据操作必须先有书面方案、备份与回退路径，并由有权限的人人工确认。

### G3：任务与实现

按已确认方案拆成可验证的功能集成或里程碑切片，维护任务、进度、决策、REQ 和状态。实现阶段不采用“每改一步就跑大量完整测试”的固定节奏；保留必要的局部快速检查，把系统测试、回归、构建、发布前验收集中到风险事件、功能集成、里程碑和版本完成阶段。未完成检查记录在 [`templates/level2/pending-verification.md`](templates/level2/pending-verification.md)。

### G4：验证与发布 readiness

按风险安排单元、集成、Contract、端到端、兼容、权限、安全、备份恢复和人工 UAT。认证、权限、支付、生产数据、迁移、密钥和发布动作必须在操作前做针对性验证并经人工 Gate。发布 readiness 必须包含版本范围、测试证据、配置字段、备份、回滚、冒烟、观察窗口、负责人和运营/支持责任。

### G5：上线、观察与运营

生产发布由有权限的人批准和执行；AI 只准备清单、命令、风险分析和证据。上线后运行核心路径冒烟，在观察窗口检查日志、错误率、延迟、资源、业务指标和用户反馈；达到阈值按既定回滚计划止损。每次版本和事故保留结果、遗留项、技术债和下一轮需求。

## 4. 验证与文档同步

实现不等于完成。每个跨模块需求都要有写入入口、读取/刷新/恢复路径、失败/取消/回滚、安全边界、未受影响模块和行为验收矩阵。文档、决策、进度、状态和验证证据在变化发生时同步维护。未运行的检查必须标为待验证，并写明真实环境、凭据或平台条件。

## 5. 退出与降级

如果项目最终只是离线、静态或可下载交付物，可以在事实记录和负责人确认后降为 LEVEL 1；如果改动转为参与他人/团队/开源仓库，则改按 LEVEL 3 接管。等级变化必须有状态迁移或重确认记录，不以“可以更新”作为依据。
