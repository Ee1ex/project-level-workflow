# ELX Level 2.0 Release Readiness

## 范围与状态

本次把 `Project Level Workflow 1.0` 完整改名并升级为 `ELX Level 2.0`。新 Skill、安装目录、状态目录和项目文档目录分别使用 `elx-level`、`.elx-level` 与 `docs/elx-level`；四级模型、执行策略和 GitHub Gate 不重新设计。

远程确认状态：`not authorized`。本记录不表示已经 Push、创建或合并 PR、改名仓库、创建 `v2.0` Tag 或发布 Release。

## 分支与提交

- 基线：`origin/main@b5eefb0ee9c74cc9bc13335db6e8e582993810d9`。
- 工作区：本地隔离工作树，不把个人绝对路径写入公共记录。
- 分支：`codex/elx-level-2-0`。
- 提交：最终验证后回写完整清单。

## 文件范围

- 公共身份与版本：`VERSION`、Schema、evals、Skill、Changelog。
- 状态迁移：CLI、状态测试、`LEVEL.md` 与状态协议。
- 生命周期：PowerShell/POSIX 安装、更新、卸载及集成测试。
- 平台适配：Codex、Claude Code、Cursor 模板、托管标记和包完整性。
- 公开文档：双语 README、两份 SVG、引用、许可证和本 readiness。

## 迁移与兼容

- 旧 `.project-workflow` 仅由 `migrate` 一次性复制到 `.elx-level`。
- 旧目录内容保持不变；复制后的新状态刷新为 workflow `2.0`。
- 新旧目录并存时返回冲突并停止，不覆盖任何一方。
- 新安装写入 `elx-level`；旧 `project-level-workflow` Skill 只提示并保留。
- `v1.0`、旧 Tag、Release、安装和状态均不在本次删除范围。

## 测试证据

已完成的分任务证据将在全量验证后汇总。尚未执行或尚未回写的检查不得视为通过，包括完整 unittest、Doctor、包校验、LEVEL 1–4 三平台冒烟、旧目录 SHA-256 对比和最终 Diff 检查。

## PR 与 Merge 方案

- PR：base `main`，head `codex/elx-level-2-0`。
- 建议标题：`feat: rename Project Level Workflow to ELX Level 2.0`。
- Review 重点：一次性状态复制、旧目录不变、新旧冲突停止、旧 Skill 保留、三平台路径及公共品牌一致性。
- 建议 Squash Merge；合并后必须回读 main Commit 与树，禁止 Force Push 或改写公共历史。

## Tag 与 Release 草案

- Tag：`v2.0`，目标必须是验证后的 main 合并 Commit。
- Release 标题：`ELX Level 2.0`。

### Release 正文

ELX Level 2.0 是 Project Level Workflow 1.0 的新品牌版本。它保留个人开发优先的 LEVEL 1–4 模型、双层项目记忆、完整 L2 PVS、L3 复用优先、L4 外部能力路由，以及统一的 GitHub 交付确认边界。

本版本将 Skill 和安装目标切换为 `elx-level`，新项目状态写入 `.elx-level`，项目摘要写入 `docs/elx-level`。`migrate` 可以把旧 `.project-workflow` 一次性复制到新目录，旧目录保持不变；新旧目录并存时停止且不覆盖。公共版本与 workflow 版本为 `2.0`，schema 保持 `2.0`。

最终 Release 将附带实际 unittest、Doctor、包校验、LEVEL 1–4、三平台适配器、状态复制、安装与 Diff 证据，以及仍待外部验证的项目。

## 回滚

- 代码通过 Revert 提交回滚，不改写历史。
- `v1.0` 与旧仓库历史保留为旧品牌稳定基线。
- 项目状态可继续读取保留的 `.project-workflow` 原目录；不要在新旧目录冲突时自动选择或覆盖。
- 仓库改名后如需回滚，使用 GitHub 仓库设置恢复旧名并回读默认分支、Commit、PR、Tag、Release 与链接。

## 未验证项

- 最终全量测试与冒烟证据尚待 Task 6 执行并回写。
- Bash/POSIX 生命周期脚本的真实运行环境能力尚待检测。
- 真实 Codex、Claude Code、Cursor 用户级安装与触发行为尚未执行。
- GitHub 仓库改名、CI、PR、Merge、Tag 和 Release 均未执行。
