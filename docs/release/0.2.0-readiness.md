# Project Level Workflow 0.2.0 发布准备记录

- 状态：本地验收完成；外部平台待采样
- 负责人：项目维护者
- 关联 Gate：四级模型发布前检查
- 最后更新时间：2026-08-12

## 发布范围

本版本把三级模型迭代为四级模型：LEVEL 1 快速验证与轻量交付、LEVEL 2 自有项目可持续运营、LEVEL 3 已有与开源项目改进、LEVEL 4 复杂项目需求分析。LEVEL 1/2 通过桥接文档分层复用 `project-vibe-spec`。

## 兼容与安全

- 旧状态 `1→1、2→3、3→4`，迁移前写 `state.backup.json`。
- 迁移在 `STATUS.md` 记录旧/新 LEVEL、Schema 和原因，迁移后的 Gate 等待人工确认。
- 旧 LEVEL 3 改为新 LEVEL 2 需要显式重确认、批准人和原因。
- 远程 Push 和 Draft PR 权限仍默认关闭；生产、密钥、数据和公开发布仍需人工 Gate。

## 计划验证

```text
python -m unittest discover -s tests -v
python scripts/workflow.py doctor --package-root .
python scripts/workflow.py validate-package --package-root .
python scripts/workflow.py init --project <temporary-project> --level 1
python scripts/workflow.py init --project <temporary-project> --level 2
python scripts/workflow.py init --project <temporary-project> --level 3
python scripts/workflow.py init --project <temporary-project> --level 4
```

## 已执行结果

- 使用工作区 Python 3.12.13 运行完整单测：48 项通过，0 failure、0 error。
- `doctor --package-root .`：Python、Git、四份活动 SOP、桥接文档、Schema 和三平台适配器全部 PASS。
- `validate-package --package-root .`：退出码 0，包版本 0.2.0。
- 四个临时项目的 `init --level 1/2/3/4`、`validate` 和 Codex/Claude Code/Cursor 适配器渲染均退出 0。
- 三个平台 `install.ps1` DryRun 均退出 0；PowerShell 三个生命周期脚本语法解析通过。
- 旧 LEVEL 迁移、备份、STATUS 记录、迁移后 Gate 和旧 LEVEL 3 显式重确认到新 LEVEL 2 已由自动化测试覆盖。

## 未执行检查

- 当前环境没有 Bash，因此未执行三个 Shell 脚本的 `bash -n` 或实机生命周期测试。
- 未连接 Codex、Claude Code、Cursor 的真实 Agent 会话，未验证安装后的触发率和长会话恢复。
- 未调用外部生产系统、数据库、部署平台、远程 Git 写入或公开发布。
- 系统 `python` 命令指向 Microsoft Store 占位程序，验收使用工作区提供的 Python 运行时；项目逻辑本身未因此改变。

## 已知风险

旧状态迁移会改变数字等级含义，更新前应保留备份并人工确认迁移后的责任模式。旧文件名只作为兼容入口，外部平台安装后的真实触发率和长会话恢复仍需目标平台采样。
