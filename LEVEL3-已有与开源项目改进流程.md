# LEVEL 3：已有与开源项目改进流程

## 1. 文档定位

本流程适用于参与他人、团队、公司或开源仓库的 Bug 修复、体验优化、边界清晰的功能开发和贡献 PR。严格程度来自继承已有仓库责任，即使本次只改一个文件，也不能跳过基线、回归和交接。

## 2. 接管与权限

先读取 README、`AGENTS.md`/`CLAUDE.md`、`CONTRIBUTING.md`、行为准则、许可证、Issue/PR 模板、CI、发布说明和已有文档。确认仓库来源、目标分支、任务来源、负责人、保密边界、贡献协议、允许修改范围和外部写入权限。没有贡献规则或维护者方向时停在接管 Gate。

## 3. 项目地图与修改前基线

用 [`templates/level3/project-map.md`](templates/level3/project-map.md) 记录项目目标、技术栈、启动/测试/Lint/构建命令、相关目录、关键调用链、数据边界、CI、发布、回滚和未知区域。修改前运行项目已有基线检查；不能安装核心依赖、初始化 Git 或访问生产环境而不先确认。

## 4. 问题、方案与影响分析

用 [`templates/level3/change-proposal.md`](templates/level3/change-proposal.md) 记录当前行为、期望行为、复现证据、根因、范围、不做、兼容性和回退。比较最小修复、局部改进和结构性方案，列出受影响接口、数据、配置、依赖、平台、文档和测试。公共 API、数据、认证、权限、安全、依赖和发布变化须由维护者/负责人确认。

## 5. 开发与回归

开始前建立可重复失败证据：优先失败测试，不能自动测试时记录人工复现步骤。按最小兼容改动推进，运行受影响测试和项目要求的完整检查，检查 Diff，避免无关重构。回归报告必须保留：修改前基线、问题复现/失败测试、修复证据、受影响回归、CI 状态、人工路径和未验证项。

## 6. Review、PR 与交接

PR 至少说明问题、关联 Issue、方案、范围、不做、测试命令和结果、兼容性、迁移/回退、截图或人工路径以及请 Review 的位置。遵守仓库分支、Review、CI、发布和许可证规则；未经授权不推送、不创建公开评论、不合并、不转 Ready、不发布。交接记录要说明已改文件、证据、遗留风险、维护者决定和下一步。

## 7. 必须停止的情况

找不到或无法复现问题、需求与仓库设计冲突、维护者未确认方向、需要数据库/认证/权限/支付/公共 API/安全策略变化、需要生产权限或不可逆操作、测试失败原因不明、回滚不可用或范围扩大时，停止实现并提交事实、方案和待批准动作。
