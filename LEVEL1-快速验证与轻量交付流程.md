# LEVEL 1：快速验证与轻量交付流程

## 1. 文档定位

本流程适用于快速验证想法、离线工具、命令行工具、脚本、Skill、插件、游戏 Mod、原型 Demo、静态页面、展示页、活动页、个人页面和没有用户系统的静态 Web 工具。也适用于重新打包并上传新版本供用户下载、但不承担持续在线服务、用户数据和运行稳定性责任的项目。

核心判断是：项目可以更新，不等于项目需要持续运营。只要交付物是本地运行、静态托管或版本化下载，且没有用户账户、云端业务状态、生产数据或持续在线服务责任，默认仍是 LEVEL 1。

本流程使用 [`references/project-vibe-spec-bridge.md`](references/project-vibe-spec-bridge.md) 定义的 PVS-Lite；原始 `project-vibe-spec` 规则和项目已有规则优先。

## 2. 不适用范围

以下情况应重新评估等级：

- 自己负责的线上产品需要持续运营、监控、备份、回滚、反馈或用户支持：推荐 LEVEL 2。
- 参与他人、团队、公司或开源仓库，必须遵守现有基线、CI、Review、PR 或交接规则：推荐 LEVEL 3。
- 大型产品、多系统编排、复杂自动化或完整运营自动化，当前只需要分析：推荐 LEVEL 4。
- 认证、权限、支付、生产数据、密钥、不可逆操作等风险动作：即使保持 LEVEL 1，也要按 R3/R4 设置人工 Gate。

## 3. PVS-Lite 最小底座

必须保留：

- 项目规则、根目录 `AGENTS.md` 和 `DOCUMENT_MAP.md`；先读取已有规则和文档，不创建平行事实源。
- Project Brief：目标用户/场景、核心路径、必做、不做、平台、交付方式和验收标准。
- `docs/project-workflow/STATUS.md`、`.project-workflow/state.json` 和 `state.backup.json`。
- 必要的决策、风险和 `pending-verification.md`；未运行的检查只能记录为待验证。

通常可以跳过或延后完整 `Requirements/LEDGER.md`、逐条 REQ、全套 PDD/PRD、复杂业务流、UI Guide、技术规格和独立功能进度文档；需求跨模块、涉及持久化或风险较高时再补充。

## 4. 流程

`目标澄清 → 轻量设计 → 实现—运行—观察—调整 → 集中验收 → 打包/交付`

### 4.1 目标澄清 Gate

在 [`templates/level1/project-brief.md`](templates/level1/project-brief.md) 中写清一句话目标、目标用户、唯一核心路径、必做功能、明确不做、平台、交付方式和验收标准。确认后再开始实现。

### 4.2 轻量设计

只记录完成核心路径所需的操作步骤、关键状态、视觉方向、技术结构、数据保存方式、构建/打包方式和第三方依赖风险。没有实际需要时不建设数据库、登录或服务端。

### 4.3 开发循环

以真实体验为主：选择一个最小可观察目标，实施后运行或体验，记录问题，再调整。每轮保持范围小而明确；不要求每个小步骤都写测试、跑完整回归或创建独立 Review。核心路径、异常状态和高风险动作仍需有可验证证据。

### 4.4 集中验收与交付 Gate

项目完成或准备打包时集中执行：

- 核心路径冒烟，包括空输入、错误输入、重复操作和重新打开后的行为；
- 构建、打包、导出或安装检查；
- 必要的目标平台人工验收和隐私/密钥检查；
- 将未运行的测试、平台回归、兼容性和发布检查写入 [`templates/level1/pending-verification.md`](templates/level1/pending-verification.md)。

待验证事项不能伪装成通过；交付记录必须写明限制、回退方式和后续检查条件。

### 4.5 发布与反馈

静态托管、上传下载包、版本发布和宣传都要由负责人确认公开内容。发布后集中收集 Bug、体验问题和功能建议；如果反馈让项目承担持续在线、用户数据或支持责任，重新评估为 LEVEL 2。

## 5. 仍然必须人工确认的动作

新增账户/数据库/权限/支付、收集个人信息、访问密钥、删除数据、写入外部账号、生产部署、公开发布和不可逆操作不能因为 LEVEL 1 轻量而跳过事前验证和人工 Gate。
