# LEVEL 3：持续运营产品开发流程总览

## 1. 文档定位

本流程适用于需要长期运营、持续迭代、多人或多 Agent 协作，并对可靠性和可维护性负责的成熟产品，例如：

- 手机 App、Web SaaS 平台、电脑客户端
- 微信小程序、持续运营的网站与服务
- 包含用户体系、权限、支付、重要数据或复杂业务流程的产品
- 修改一个模块可能影响多个模块，需要定位、回滚和复盘的项目

LEVEL 3 的目标是让每次变更都具备明确边界、固定产物、验证证据和人工责任，并让项目在上线后仍然可监控、可维护、可持续运营。

## 2. 核心原则

1. **先验证需求，再建设系统。** 没有真实痛点、目标用户和成功指标，不进入完整开发。
2. **MVP 只保留一条核心闭环。** 用户有需求、完成关键操作、得到结果、团队收集反馈。
3. **范围冻结。** 本版本做什么和不做什么同时写清楚，新增想法进入后续 Backlog。
4. **文档是上下文，不是装饰。** PRD 说明做什么，技术方案说明怎么设计，任务卡说明本轮怎么改。
5. **一次只交付一个可验证纵向切片。** 不同时铺开大量无法验收的半成品。
6. **风险决定自动化边界。** AI 可以分析、起草、检查和实施低风险任务，但方向、数据、安全和生产发布由人负责。
7. **代码能运行不等于完成。** 测试、文档、迁移、监控、回退和进度必须同步。
8. **上线不是终点。** 日志、指标、反馈、事故和复盘进入下一轮需求与规则。

## 3. 总体流程与人工 Gate

`机会与调研 → 需求分析 → 产品与体验设计 → 技术设计 → 版本切片 → 开发 → 验收 → 上线 → 监控运营 → 宣传增长 → 复盘迭代`

| Gate | 人工确认内容 | 未通过时 |
| --- | --- | --- |
| G0 立项 | 真实问题、目标用户、价值与投入值得继续 | 等待、补调研或放弃 |
| G1 需求 | PRD、MVP 范围、不做清单、验收标准 | 不做原型和技术方案 |
| G2 体验 | 用户流程、原型、关键状态和视觉方向 | 不进入详细技术设计 |
| G3 技术 | 架构、API、数据、安全、风险和回退 | 不拆开发任务 |
| G4 任务 | 当前切片范围、验收、验证命令和风险等级 | Agent 不得执行任务 |
| G5 合并 | CI、测试、Review、文档和迁移证据 | 不合并主分支 |
| G6 发布 | Readiness、备份、回滚、冒烟测试和负责人 | 不部署生产 |
| G7 宣传 | 产品已可用、口径准确、素材无隐私问题 | 不公开发布内容 |

### 3.1 工具治理与选择原则

LEVEL 3 可以使用更多工具，但每个工具都必须有明确的输入、产出、权限和退出条件。工具能力不能绕过 G0～G7，也不能成为改变技术栈的默认理由。

- **Skill**：分析或执行方法，例如 `grill-me`、Product Design、Qima 系列 Skill。
- **Plugin**：Skills、MCP Server、App 等能力的组合，例如 GitHub、Supabase、Vercel、Netlify；使用前检查安装、登录、授权和环境范围。
- **MCP / App**：连接外部数据和系统，读操作也要遵守保密范围，写操作必须确认目标与内容。
- **CLI / Web 服务**：运行本地命令、部署或制作宣传素材；命令副作用、账号写入、生产发布必须人工批准。
- **Qima 特别规则**：Agent 只在产物和技术栈匹配时提醒你“可以手动使用哪个 Qima Skill”，不得直接调用，也不得自动串行执行完整 Qima 流程。

| Gate / 阶段 | 可选工具 | 使用条件 | 主要产出 | 必须保留的人工责任 |
| --- | --- | --- | --- | --- |
| G0～G1 调研与需求 | `grill-me`；Qima `vibe-idea` 提醒 | 假设多、需求无法收敛或需要系统澄清 | 未决问题、PRD 输入、范围取舍 | 立项、优先级和范围由你决定 |
| G2 产品体验 | Product Design；Qima `vibe-interaction`、`vibe-design`、`vibe-prototype` 提醒 | 有真实用户流程、视觉目标或原型需求 | 流程、状态、视觉规范、可交互原型 | 视觉方向和体验验收由你确认 |
| G3 技术设计 | Supabase、Vercel、Netlify；Qima 架构/实现提醒 | 完成平台比较并记录 ADR | 架构、数据、部署和安全合同 | 技术栈、数据库、权限和费用由负责人批准 |
| G4～G5 开发合并 | GitHub；安全 PowerShell；浏览器与代码审查工具 | 任务卡 Ready、仓库权限明确 | 分支、PR、CI、测试和 Review 证据 | 合并、高风险修改和外部写入需批准 |
| G6 上线 | Vercel 或 Netlify；GitHub CI/CD | 已有 Preview、回滚和监控方案 | 部署记录、冒烟结果、Release | 生产发布和密钥操作由有权限的人执行 |
| G7 宣传 | 生图、视频总结、Remotion/视频制作、自媒体上传工具 | 产品可用、传播口径已确认 | 封面、图文、视频和发布草稿 | 隐私、承诺、账号和最终公开发布由你确认 |

### 3.2 其他按需扩展工具

| 能力缺口 | 可选工具 | 采用条件与边界 |
| --- | --- | --- |
| 工程规则初始化 | `web-project-standards` | 新 Web 项目建立 `AGENTS.md`、`CLAUDE.md` 等规则时使用；现有项目先合并既有规范，不创建平行标准。 |
| 计划、TDD、调试与 Review | Superpowers 对应 Skills | 用于 G4～G5 的复杂任务；其流程不能替代本文件的风险分级和 G0～G7。 |
| 前端实现与体验打磨 | `frontend-design`、`ui-ux-pro-max`、`make-interfaces-feel-better` | G2 已批准视觉和交互目标后使用，输出必须回写权威 `DESIGN.md` 或实现任务。 |
| 参考站设计系统提取 | `firecrawl-website-design-clone` | 只对有权访问和合理使用的页面操作；提取用于研究与设计规范，不直接复制品牌资产或受保护内容。 |
| 浏览器验收与工具封装 | `agent-browser`、`cli-anything` | 前者用于可重复 E2E 证据；后者仅在确实需要把软件能力封装为可测试 CLI 时使用。 |
| Skill 发现、管理与优化 | `find-skills`、`skill-manager`（可用时）、`darwin-skill` | 只在能力缺口明确时使用；安装、升级或自动优化后必须重新审查权限、行为和回归结果，不直接作用于生产。 |
| 代码与知识收尾 | `clean-code-reviewer`、`neat-freak` | 前者辅助代码质量 Review，后者核对代码、文档、任务、规则和进度的一致性；工具建议需人工复核。 |
| 宣传素材 | `gpt-image`、`guizang-social-card-skill`、`video-podcast-maker`、`remotion-best-practices`、`html-ppt`、`museon-cli` | G7 前只生成候选素材与调研结果；公开文案、素材和账号发布仍经人工批准。 |

该表是能力缺口到工具的路由，不是安装清单。已有工具能完成任务时不重复引入同类工具；候选 Skill 不可用时，先使用通用人工流程，不因安装工具阻塞项目关键路径。

每个版本建议在 `tools-and-providers.md` 或相关 ADR 中记录：选用了什么、为什么选、读取与写入权限、输入输出、数据去向、费用与配额、替代方案、停用与迁移方式。未安装的 Plugin 只是候选项，选定后再安装和授权。

## 4. 阶段零：机会判断与前期调研

### 目标

避免因为技术冲动立项，先确认“谁需要、为什么现在需要、为什么会选择我们”。

### 调研内容

- 目标用户、典型场景和当前替代方案
- 问题发生频率、严重程度和付费或持续使用意愿
- 竞品核心路径、定价、评价和明显缺口
- 渠道可达性：如何找到第一批用户
- 商业目标：收入、获客、作品集、内部提效或战略价值
- 技术、数据、平台审核、内容和合规风险
- 7 天内可以验证的最小实验或原型

### 产出：`idea-review.md`

- 执行、等待或放弃
- 真实痛点与证据
- 目标用户和场景
- 现有替代方案
- 核心价值假设
- MVP 核心闭环
- 成功指标与停止条件
- 产品、技术、商业和合规风险

### G0 立项 Gate

只有你明确选择“执行”，才进入需求分析。AI 是立项审计和研究助手，不代替你承担产品方向判断。

当产品方向存在多个合理分支时，可以使用 `grill-me` 逐轮追问真实用户、价值、约束、失败条件和取舍；共同理解未确认前不进入设计。若希望用结构化方法整理模糊想法，Agent 可以提醒你手动考虑 Qima `vibe-idea`，但不会直接调用。

## 5. 阶段一：需求分析与版本范围

### 目标

将想法转为可设计、可开发和可验收的版本合同。

### PRD 最小内容

1. 背景、问题和目标
2. 目标用户与核心场景
3. 用户故事和核心用户路径
4. 本版本功能范围
5. 明确不做的内容
6. 业务规则、权限和异常情况
7. 数据与隐私要求
8. 非功能要求：性能、可靠性、安全、兼容性和可访问性
9. 验收标准 AC
10. 成功指标、埋点和反馈方式
11. 依赖、风险和待确认事项

### 范围冻结方法

- 用 Must、Should、Could、Not Now 标注优先级。
- MVP 只保留一条能够验证价值的端到端路径。
- 新需求先记录，不在当前版本中“顺手加入”。
- 需求变化影响架构、数据或排期时，先更新 PRD 和决策记录，再修改代码。

### G1 需求 Gate

PRD、验收标准和不做清单未经你确认，不进入原型设计。

G1 前再次使用 `grill-me` 的条件是仍存在会改变范围、数据、交互或商业模式的未决选择；不要把它用于每个文案或样式小问题。若手动采用 Qima `vibe-idea`，其 `idea.md` 只能作为 PRD 输入之一，最终仍以本项目批准的 PRD 为权威来源。

## 6. 阶段二：产品、交互与视觉设计

### 目标

先验证产品结构和全部用户状态，再让 Agent 写正式前端代码。

### 推荐顺序

1. 信息架构：页面、导航、功能归属和层级。
2. 用户流程：从入口到完成核心任务的步骤。
3. 状态设计：默认、加载、空、成功、失败、无权限、离线和过期。
4. 低保真原型：验证流程、信息和操作，不追求视觉细节。
5. 可交互原型：使用模拟数据验证动态行为。
6. 视觉设计系统：颜色、字体、间距、组件、图标、动效和响应式规则。
7. 可用性验证：邀请目标用户完成核心任务并记录阻塞点。

### 产出

- `page-flow.md`：页面和用户路径
- `information-architecture.md`：信息架构
- `states.md`：关键状态与异常流程
- `DESIGN.md`：视觉和组件规范
- 可点击原型或 `wireframe.html`
- 可用性问题与修改记录

### 设计工具路由

- Product Design 用于研究、体验审计、视觉方案探索、从已选视觉稿实现界面或 Design QA。没有 URL、截图、草图或明确目标时，先完成视觉方向确认，不让工具凭空决定品牌。
- 若交互状态很多，Agent 可以提醒你手动考虑 Qima `vibe-interaction`，用来补全页面、状态和交互契约。
- Qima `vibe-design` 依赖用户提供的视觉来源，适合整理和校验 `design.md`，不负责凭空选择视觉风格。
- Qima `vibe-prototype` 依赖 Stitch MCP，并包含人工在 Stitch 中生成原型的环节，不应描述成一键自动完成。
- Product Design 与 Qima 同时使用时，Qima 负责结构化产品/交互产物，Product Design 负责视觉探索和体验验证；同一信息只保留一份权威文档，避免两个工具输出互相覆盖。

### G2 体验 Gate

你确认核心流程、页面状态和视觉方向后，才进入详细技术方案。原型未覆盖错误、空数据和权限状态时，不视为完成。

## 7. 阶段三：技术、数据与安全设计

### 目标

把已确认的产品需求转为开发可以遵循的技术合同。

### 技术方案内容

- 系统边界、架构图和模块职责
- 技术栈、依赖选择及依据
- 核心数据流和失败处理
- API 契约、错误码、鉴权和版本策略
- 数据模型、索引、约束、迁移、备份和恢复
- 缓存、队列、第三方服务和降级策略
- 配置、密钥、环境和权限管理
- 性能容量、可用性和扩展假设
- 日志、指标、追踪、告警与审计
- 测试策略、发布方式、灰度和回滚
- 风险清单、替代方案和待确认事项

### 技术平台选择 Gate

在详细架构前先完成平台比较，并把选择写入 ADR。至少回答：数据放在哪里、谁负责认证、前后端如何部署、环境如何隔离、如何备份回退、费用和配额如何控制、Provider 失败时怎么办。

| 路由 | 适用条件 | 可以提醒使用的能力 | 约束 |
| --- | --- | --- | --- |
| Qima 固定栈路由 | 明确选择 Next.js App Router、Tencent CloudBase、shadcn/ui，且团队接受 Qima 的约定 | 可提醒你手动考虑 `vibe-architecture` 和 `vibe-implement` | 仍须通过 G3/G4；Qima 不由 Agent 直接调用 |
| Supabase 路由 | 需要 PostgreSQL、认证、数据库或对象存储，并接受其服务边界 | Supabase Plugin 作为数据、认证和存储候选 | 数据模型、RLS/权限、迁移、备份、地域与费用必须 Review |
| Vercel 路由 | Web 框架和团队发布方式适配 Vercel | Vercel Plugin 用于 Preview、环境配置和部署候选 | 先 Preview；生产发布与环境变量写入需人工批准 |
| Netlify 路由 | 静态站点或 Web 框架和团队发布方式适配 Netlify | Netlify Plugin 用于 Preview、配置、Functions 和部署候选 | 先 Preview；生产发布与环境变量写入需人工批准 |
| 通用/既有架构路由 | 已有系统、非 Qima 栈或另有组织标准 | 使用项目自己的架构、Provider 和部署流程 | 保留 PRD、交互、API、数据、安全、测试和回滚契约，不套 Qima 固定实现 |

Supabase 解决数据、认证或存储问题，Vercel/Netlify 主要承担 Web 构建与部署，它们不是同一维度的互斥选项；但 Vercel 与 Netlify 默认选择一个主部署平台。只有存在明确的多平台容灾或渠道需求时才同时维护两套，并计算额外成本。

### 设计规则

- 前后端共享 API Contract 和推导类型，前端不手写后端响应类型。
- 数据库表必须人工 Review，既不为未来幻想过度设计，也避免核心业务频繁破坏性改表。
- 公共接口和数据迁移优先保持向后兼容。
- 第三方 Provider 必须考虑超时、失败、空数据、限流和费用上限。
- 所有不确定且会影响范围、架构、数据或安全的选择标记为“需要人工确认”。

### 产出

- `tech-spec.md`
- `api-spec.md` 或可生成代码的 Contract
- `db-schema.md` 与迁移草案
- `security-privacy.md`
- `risk-checklist.md`
- `test-strategy.md`
- `release-strategy.md`

### G3 技术 Gate

技术方案和平台 ADR 未经批准，不创建正式数据库迁移，不安装或授权相关生产 Plugin，不拆开发任务，也不让 Agent 猜测架构开工。选择非 Qima 固定栈时，后续走通用架构与实现流程；不得仅替换 Provider 名称后继续套用 Qima 的架构假设。

## 8. 阶段四：文档治理与版本切片

### 项目上下文入口

在仓库根目录维护 `AGENTS.md`，只规定：

- Agent 行为、读取顺序和修改边界
- 项目命令、代码规范和依赖方向
- 风险分级、人工审批和完成标准
- Git、测试、文档与安全规则

产品细节不要重复抄进 `AGENTS.md`，应由 PRD、技术方案和任务卡维护，防止多份文档互相冲突。

### 推荐文档分类

| 类型 | 用途 |
| --- | --- |
| PRD / REQ | 产品范围、需求和验收标准 |
| DEV | 架构、模块和复杂功能技术方案 |
| BIZ / ADR | 业务或架构决策及选择依据 |
| TASK | 当前纵向切片的执行合同 |
| BUG | 缺陷、证据、严重程度和关联需求 |
| PROG | 已完成、阻塞、风险和下一步 |
| OPS | 部署、回滚、监控和事故处理 |

### Phase 与纵向切片

Phase 用于管理版本里程碑，不应机械固定为前端阶段、后端阶段。推荐示例：

| Phase | 目标 | DoD 示例 |
| --- | --- | --- |
| Phase 0 | 项目与文档初始化 | AGENTS、PRD、技术方案、目录和 CI 骨架就绪 |
| Phase 1 | 可运行骨架 | 服务可启动、配置可读、基础监控可用 |
| Phase 2 | 核心闭环 A | 一条真实端到端用户路径可验收 |
| Phase 3 | 核心闭环 B | 第二条业务路径可验收且不破坏 A |
| Phase 4 | 发布准备 | 测试、迁移、文档、回滚和冒烟检查就绪 |

每个 Phase 再拆成小型纵向任务，一次贯穿必要的数据、服务、API、UI、测试和文档，交付可演示结果。

### 每张任务卡必须包含

- 任务目标和关联需求
- 范围与明确不做
- 允许修改和禁止修改的文件或模块
- 前置依赖
- 验收标准
- 验证命令
- 风险等级与自动化等级
- 人工 Gate
- 文档、监控和回退要求

### 任务风险等级

| 风险 | 示例 | Agent 权限 |
| --- | --- | --- |
| R1 低风险 | 文档、文案、独立样式、小型测试 | 可执行并提交证据 |
| R2 普通风险 | 边界清晰的业务逻辑和内部 API | 可修改，但必须测试和 Review |
| R3 高风险 | 数据库、认证授权、公共 API、核心依赖、部署配置 | 修改前和合并前均需人工批准 |
| R4 关键风险 | 生产数据删除、密钥、支付资金、不可逆迁移、生产发布 | AI 只分析和准备命令，不得自主执行 |

### G4 任务 Gate

只有经你确认并进入 Ready 状态的任务，Agent 才能执行。一个对话优先完成一个任务或少量强相关任务，避免上下文持续膨胀。

## 9. 阶段五：开发实施

### 开发顺序

1. 读取 `AGENTS.md`、任务卡、相关 PRD、技术方案和现有代码。
2. 建立功能分支或独立 worktree，记录修改前测试基线。
3. 先补测试或写出可重复的失败证据。
4. 按最小纵向切片实施数据、服务、API、UI 和文档。
5. 每完成一个小步就运行对应测试，不在最后集中发现全部问题。
6. 检查 Diff，删除无关改动，更新任务进度和风险。
7. 创建聚焦的 Pull Request，等待 CI 和 Review。

### 强制边界

- 不超出任务卡范围，不顺手加功能或无关重构。
- 发现设计不成立时，先更新并审批文档，再继续编码。
- 数据库变化先设计迁移、约束和回退，再改 ORM、服务、API 和 UI。
- 配置统一管理，不在业务代码中随意读取环境变量。
- API 不直接承担数据库和第三方数据源的全部职责，应保持明确分层和可测试边界。
- 不提交密钥、生产数据、日志、备份、依赖目录和构建产物。
- 在 Windows 中由 Agent 调用 PowerShell 或原生命令时，遵循安全 PowerShell 规范处理参数、路径、引号、编码和进程启动；不得拼接未验证输入或用跨 Shell 命令绕过边界检查。
- GitHub 工具可以读取 Issue、PR、Review 和 CI，也可以在确认后辅助创建分支、PR 或处理反馈；推送、公开评论、合并、Release 和仓库设置变更前必须展示目标与内容。

## 10. 阶段六：测试、验收与代码审查

### 测试层次

- 单元测试：核心规则、边界和错误处理。
- 集成测试：数据库、缓存、队列和第三方适配器。
- Contract 测试：前后端字段、错误码和兼容性。
- 端到端测试：核心用户路径和关键平台。
- 手工验收 UAT：由产品责任人按 AC 操作并记录证据。
- 非功能测试：性能、安全、权限、兼容、可访问性、备份恢复。

### PR Review 内容

- 正确性、需求覆盖和异常情况
- 架构边界、耦合和公共接口影响
- 数据迁移、权限、安全和隐私
- 性能、资源使用和第三方失败处理
- 测试是否能够证明行为，而不是只追求覆盖率
- 文档、配置、监控和回滚是否同步

### G5 合并 Gate

以下条件全部满足后才能合并：

- [ ] 验收标准逐项有证据
- [ ] 必需 CI、测试、Lint、类型检查和构建通过
- [ ] 阻塞 Review 意见已处理
- [ ] 数据迁移可从上一版本升级，并有备份或回退方案
- [ ] 外部服务失败、超时和空状态已处理
- [ ] 文档、进度和已知问题已更新

## 11. 阶段七：上线发布

### 发布准备产出

- `deploy-readiness.md`
- `rollback-plan.md`
- `smoke-test.md`
- Release Notes 与已知问题
- 数据迁移、备份和恢复记录
- 发布负责人、观察窗口和紧急联系人

### 上线前检查

- [ ] 前后端构建与完整 CI 通过
- [ ] 配置和环境变量字段完整，生产密钥未进入 Git
- [ ] 数据库迁移已在测试或预发布环境验证
- [ ] 备份可用，回滚条件和步骤明确
- [ ] 域名、证书、网络端口和内部服务暴露合理
- [ ] 日志、指标、告警和健康检查已启用
- [ ] 核心功能冒烟用例已准备
- [ ] 客服、运营和宣传口径已同步

### G6 发布 Gate

生产发布必须由有权限的人批准和执行。AI 可以生成检查清单、命令和风险分析，但不得自行承担生产部署、密钥操作或不可逆数据变更。

Web 产品使用 Vercel 或 Netlify 时，默认顺序为“本地验证 → Preview 部署 → Preview 冒烟/UAT → G6 → 生产部署 → 生产冒烟与观察”。GitHub CI/CD 可以自动检查和准备发布，但不得绕过 G6。Supabase Schema、RLS/权限、迁移或生产数据操作按 R3/R4 管理，并在 Preview/测试环境先验证。

### 发布后

1. 立即运行冒烟测试。
2. 在观察窗口查看错误率、延迟、资源、业务转化和关键日志。
3. 达到回滚阈值时按计划回滚，不在事故中临时发明流程。
4. 记录发布时间、版本、执行人、结果和异常。

## 12. 阶段八：监控、运营与维护

### 日常巡检

- 服务健康、5xx、404/502 异常和客户端崩溃
- 登录、支付、通知、AI 调用和第三方 Provider 失败
- 数据库连接、慢查询、磁盘、内存和容器重启
- 关键业务指标、转化漏斗和用户反馈
- 依赖安全公告、证书、配额和费用异常

### 处理分级

- 信息：归档，用于趋势观察。
- 警告：进入技术债或下一迭代。
- 错误：创建 Bug 和修复任务，评估是否影响发布。
- 严重：人工接管，执行事故响应、止损和回滚。

### 复盘

每周、每次 Release 和每次 Incident 后记录：

- 交付了什么，有哪些指标变化
- 哪些事情失败，证据和根因是什么
- 剩余风险和技术债
- 下一轮需求和优先级
- 哪些规则需要写入 AGENTS、文档、测试或自动化

## 13. 阶段九：宣传与增长

### 宣传前提

产品核心路径可用、承诺与实际能力一致、反馈入口和隐私说明就绪。仍在实验的功能必须明确标注，不把 Roadmap 当作已交付能力宣传。

### 发布素材包

- 产品定位：为谁解决什么问题
- 核心价值主张和差异点
- 产品截图、演示动图和品牌封面
- 30～60 秒短演示和 3～10 分钟完整介绍
- Bilibili 视频脚本、标题、简介和章节
- 个人博客文章、更新日志和技术复盘
- 官网或落地页、GitHub README、FAQ 和反馈入口

### 内容主线

`用户问题 → 现有方法的困难 → 产品如何操作 → 用户得到什么结果 → 如何开始使用`

技术文章可以补充架构选择、开发挑战和复盘，但面向普通用户的公开页面只保留用户能感知的价值、动作和结果，不加入内部制作过程和开发备注。

### 可选宣传工具链

| 工作 | 可选工具或流程 | 使用说明 |
| --- | --- | --- |
| 封面、配图与图文总结 | [Smart Draw 生图总结流程](https://smart-draw.weijordan.cn/)、`gpt-image`、`guizang-social-card-skill` | 使用批准的品牌规范和真实产品截图；不得生成不存在的功能。Smart Draw 的具体能力使用前实际验证。 |
| 视频资料总结 | [视频总结参考流程](https://ncn3ig4zbrs7.feishu.cn/wiki/GoWIwKra7irj4iksZtDcD6mBn9d) | 可接入团队批准的兼容大模型；若考虑 DeepSeek 等模型，需评估质量、成本、隐私、数据保留和 API 可用性。链接内容使用前再次确认。 |
| 视频与动效制作 | `video-podcast-maker`、`remotion-best-practices` 或团队剪辑工具 | 先批准脚本、演示账号和素材，再生成旁白、字幕、章节和画面。 |
| 多渠道改写 | `li-distribute`、`li-cover`、`content-strategy` | 分别适配 Bilibili、个人博客、GitHub 和其他渠道，并保持产品事实一致。 |
| 自动上传 | [自媒体平台自动化上传参考](https://www.bilibili.com/video/BV1yMNZ6DEfg/) | 默认生成上传包或草稿；账号授权、发布时间、可见范围、最终标题和公开发布必须人工确认。该链接所示能力需在采用前验证。 |

自动化上传属于外部写入。即使 G7 已通过，也建议首次使用新工具时先对测试账号或非公开草稿验证；任何工具都不得自动处理验证码、绕过平台安全机制或发布未经批准的版本。

### G7 宣传 Gate

检查演示账号、截图、日志、配置和视频画面中是否包含个人信息、密钥或内部数据；确认价格、功能、平台支持和上线地址准确后再发布。

### 增长闭环

为不同渠道设置可追踪链接或来源标记，观察访问、注册、激活、留存、付费和反馈质量。宣传数据进入下一轮需求判断，不以播放量替代真实产品价值。

## 14. 推荐目录结构

```text
project/
├─ AGENTS.md
├─ README.md
├─ CHANGELOG.md
├─ docs/
│  ├─ product/          # 调研、PRD、验收标准、指标
│  ├─ design/           # 流程、原型、状态、视觉规范
│  ├─ architecture/     # 技术、API、数据、安全和 ADR
│  ├─ tasks/            # backlog、ready、doing、done
│  ├─ quality/          # 测试策略、报告和缺陷
│  ├─ operations/       # 部署、回滚、监控和事故复盘
│  └─ progress/         # 版本与进度记录
├─ promotion/           # 截图、封面、视频和发布文案
└─ .github/             # Issue、PR 模板与 CI
```

目录可以按技术栈调整，但每类信息只保留一个权威来源，避免同一需求散落在多个冲突文档中。

## 15. 必须停下来询问你的情况

Agent 遇到下列情况时不得自行继续：

- 需求、验收或产品方向存在会显著改变结果的歧义。
- 需要改变范围、架构、公共 API、数据库或实施顺序。
- 涉及认证、权限、支付、隐私、合规或安全策略。
- 需要生产权限、密钥、数据删除、不可逆迁移或部署操作。
- 文档与代码冲突，且无法根据优先级判断哪一个是权威来源。
- 测试失败原因不明、回滚不可用或监控证据不足。
- 宣传内容包含未经确认的功能、定价、用户数据或商业承诺。

提问时应同时给出：已确认事实、未知项、可能影响、可选方案和推荐选择，方便你做决定。

## 16. 参考与整合说明

本流程求同存异地整合了以下资料：

- 本地《产品变现.md》：采用成熟产品的规范化开发、文档模板、最小 MVP、持续反馈，以及 GitHub、视频、封面和自媒体宣传建议。
- [如何从 0 到 1 Vibe Coding 一个项目，并长期维护](https://ai.codefather.cn/post/2077996578576056322)：采用规划优先、数据库设计 Review、先验证产品与页面、AGENTS 上下文、REQ/DEV/BIZ/BUG/PROG 文档治理、范围冻结、Phase DoD、按需加载和最小纵向切片等原则。
- [我的 AI 编程工作流：从需求判断到上线复盘](https://ai.codefather.cn/post/2075533184434438146)：采用“需求判断—原型—技术方案—任务拆解—开发—测试—Review—部署—日志—复盘”的流水线，以及固定输入输出、风险等级、自动化等级和人工 Gate。
- [Daliu Awesome Skills / Qima](https://github.com/idiotLeoLYJ/Daliu-Awesome-Skills#%E9%BD%90%E7%A0%81--qima)：参考 `idea.md → interaction.md → architecture.md → design.md → prototypes → code` 的产物链，并根据其固定技术栈增加平台选择分叉；本流程只提醒，不默认调用。
- [`grill-me` / `grilling`](https://github.com/mattpocock/skills)：参考持续追问决策树前沿、给出建议并在共同理解确认后才行动的方法。
- [Windows Agent 安全 PowerShell 调用规范](https://github.com/Misaka-Mikoto-Tech/agent-skills)：参考 Windows 原生命令参数、路径、引号、编码与进程启动规则。
- [OpenAI Developers](https://developers.openai.com/)：参考 Codex Plugin 可以组合 Skills、MCP Server 和 App 能力的说明；具体 Plugin 能力、权限和可用性以安装时的官方页面为准。
- 用户提供的《我每天在用的 19 个 Codex Skills》工具清单：按能力缺口整合工程规则、计划调试、界面实现、Skill 管理、浏览器验收、CLI 封装、生图、视频、PPT、设计系统提取和知识收尾能力，没有将整套工具设为强制依赖。

两篇文章中较重的自动化节点被保留为可逐步建设的目标，而不是要求项目第一天就全部自动化。先建立目标、计划、验收、测试和回滚，再根据真实维护成本逐步加入 CI、自动巡检、日志分析和定期复盘。

以上为面向长期运营产品的综合流程设计，不是原文逐字复刻，也不替代具体行业的法律、合规和安全审查。
